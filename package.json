{
  "name": "json-server-vercel",
  "version": "1.0.0",
  "description":"node api/server.js <-> json-server --watch db.json --port 3001",
  "main": "api/server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
